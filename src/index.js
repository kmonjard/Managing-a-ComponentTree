import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

// See CSS text-decoration Property -- https://www.w3schools.com/cssref/pr_text_text-decoration.asp
// See Generating unique ids with the UUID Package -- https://www.npmjs.com/package/uuid
